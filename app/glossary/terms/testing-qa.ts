import { GlossaryTerm } from '../types';
export const testingQaTerms: GlossaryTerm[] = [
    { slug: 'test-pyramid', title: 'Test Pyramid', category: 'Testing & QA', definition: `The test pyramid is a testing strategy that prescribes many fast, cheap unit tests at the base, fewer integration tests in the middle, and a small number of slow, expensive end-to-end tests at the top. Coined by Mike Cohn, the pyramid shape reflects the ideal ratio: many small tests, few large tests.\n\nLayers: Unit tests (test individual functions/classes in isolation, milliseconds, thousands of them), Integration tests (test component interactions, seconds, hundreds), End-to-end tests (test full user flows through the real system, minutes, dozens), and Manual/exploratory tests (human verification, rare, for subjective quality).\n\nThe anti-pattern is the "ice cream cone" — many E2E tests, few unit tests. This creates slow, flaky, expensive test suites that developers avoid running. The test pyramid keeps the feedback loop fast.`, whyItMatters: `A properly shaped test pyramid gives developers confidence to refactor and ship quickly. Fast unit tests catch most bugs in seconds. Slow E2E tests verify critical paths without becoming a bottleneck.`, faqs: [{ question: 'What is the test pyramid?', answer: 'A testing strategy: many fast unit tests (base), fewer integration tests (middle), few slow E2E tests (top). The pyramid shape keeps the feedback loop fast while maintaining coverage.' }, { question: 'What ratio should the test pyramid follow?', answer: 'No universal ratio, but a common guideline: 70% unit, 20% integration, 10% E2E. The key principle: if a bug can be caught by a unit test, don\'t write an integration test for it.' }], relatedTerms: ['unit-testing', 'integration-testing', 'e2e-testing', 'shift-left-testing'] },
    { slug: 'unit-testing', title: 'Unit Testing', category: 'Testing & QA', definition: `Unit testing is the practice of testing individual functions, methods, or classes in isolation from the rest of the system. Unit tests are the foundation of the test pyramid — fast (milliseconds), focused (one assertion per test), and numerous (thousands in a mature codebase).\n\nUnit test characteristics (FIRST): Fast (run in milliseconds), Independent (no shared state between tests), Repeatable (same result every time), Self-validating (pass/fail without human inspection), and Timely (written alongside or before the code).\n\nTools by language: JavaScript (Jest, Vitest), Python (pytest), Java (JUnit), Go (built-in testing package), Rust (built-in #[test]). Test coverage targets: 80%+ line coverage for business logic, 90%+ for critical financial/security code.`, whyItMatters: `Unit tests catch 70%+ of bugs before code reaches production. They enable fearless refactoring — change implementation, run tests, confidence that nothing broke. Without unit tests, every change is a risk.`, faqs: [{ question: 'What is a unit test?', answer: 'A test that verifies a single function or class in isolation. Fast (milliseconds), focused (one behavior per test), and numerous (thousands in a mature codebase). The base of the test pyramid.' }, { question: 'How much unit test coverage is enough?', answer: '80%+ line coverage for business logic, 90%+ for critical paths (payments, auth, security). 100% coverage is often not worth the effort — diminishing returns on testing trivial code.' }], relatedTerms: ['test-pyramid', 'integration-testing', 'tdd'] },
    { slug: 'integration-testing', title: 'Integration Testing', category: 'Testing & QA', definition: `Integration testing verifies that multiple components work correctly together — testing the interfaces and interactions between modules, services, databases, and external APIs. Unlike unit tests (isolated) or E2E tests (full system), integration tests focus on the boundaries between components.\n\nTypes: Component integration (testing a service with its database), API integration (testing client-server contract), Third-party integration (testing with external APIs using mocks or sandboxes), and Data integration (testing data flows between systems).\n\nTools: Testcontainers (spin up real databases/services in Docker for tests), Pact (contract testing for APIs), WireMock (HTTP mock server), and database-specific test utilities. Integration tests typically run in CI/CD, not on developer machines.`, whyItMatters: `Bugs at component boundaries are the most common and the most expensive to fix. Integration tests catch "works in isolation but fails when connected" bugs — the ones unit tests miss.`, faqs: [{ question: 'What is integration testing?', answer: 'Testing that verifies multiple components work correctly together. Focuses on the boundaries: service-to-database, service-to-service, client-to-API. The middle layer of the test pyramid.' }, { question: 'Integration test vs E2E test?', answer: 'Integration tests verify component interactions (service + database). E2E tests verify complete user flows through the entire system. Integration tests are faster and more focused.' }], relatedTerms: ['test-pyramid', 'unit-testing', 'e2e-testing', 'contract-testing'] },
    { slug: 'e2e-testing', title: 'End-to-End (E2E) Testing', category: 'Testing & QA', definition: `End-to-end testing verifies complete user flows through the entire application — from UI interaction to backend processing to database persistence and back. E2E tests simulate real user behavior in a real (or staging) environment.\n\nTools: Playwright (Microsoft, cross-browser, fastest), Cypress (developer-friendly, single browser), Selenium (legacy, broadest browser support), and Puppeteer (Chrome/Chromium only).\n\nE2E testing best practices: Test critical paths only (login, checkout, core workflows), keep E2E tests stable (avoid flaky selectors, use data-testid attributes), run in CI/CD (not blocking development), and limit quantity (10-50 E2E tests, not 500). Visual regression testing (Percy, Chromatic) catches UI changes that functional tests miss.`, whyItMatters: `E2E tests are the ultimate validation that your application works for real users. They catch integration failures, configuration issues, and UI bugs that lower-level tests cannot detect. But they're slow and fragile — use sparingly.`, faqs: [{ question: 'What is E2E testing?', answer: 'Testing complete user flows through the entire application — simulating real user behavior from UI to database. The top of the test pyramid: few in number, highest confidence, slowest to run.' }, { question: 'How many E2E tests should we have?', answer: 'As few as possible to cover critical paths. 10-50 for a typical application. If you have 500+ E2E tests, your test pyramid is inverted (ice cream cone) and you should convert most to integration or unit tests.' }], relatedTerms: ['test-pyramid', 'integration-testing', 'visual-regression-testing'] },
    { slug: 'shift-left-testing', title: 'Shift-Left Testing', category: 'Testing & QA', definition: `Shift-left testing is the practice of moving testing activities earlier in the software development lifecycle — from post-development QA to during and before development. The principle: the earlier you find a bug, the cheaper it is to fix.\n\nShift-left techniques: TDD (write tests before code), Static analysis (catch bugs without running code — TypeScript, ESLint, SonarQube), Code review (human review before merge), Pre-commit hooks (run linting and unit tests before commit), CI pipeline (run tests on every push), and Security scanning (SAST/DAST in CI/CD).\n\nBug cost multiplier by phase: requirements (1x), design (5x), development (10x), testing (20x), production (100x). A bug caught in development costs 10% of what it costs in production.`, whyItMatters: `The cost of fixing a bug grows exponentially the later it's found. Shifting testing left catches bugs when they're cheapest to fix — in the developer's IDE, not in production at 2 AM.`, faqs: [{ question: 'What is shift-left testing?', answer: 'Moving testing activities earlier in development — from post-development QA to during/before development. Earlier detection = cheaper fixes. Uses TDD, static analysis, pre-commit hooks, and CI pipelines.' }, { question: 'How much does a production bug cost vs a development bug?', answer: 'Industry research shows approximately 100x: a bug that costs $100 to fix during development costs $10,000 to fix in production (incident response, hotfix, customer impact, reputation damage).' }], relatedTerms: ['test-pyramid', 'tdd', 'cicd', 'dora-metrics'] },
    { slug: 'tdd', title: 'Test-Driven Development (TDD)', category: 'Testing & QA', definition: `Test-Driven Development (TDD) is a development methodology where you write a failing test before writing the code to make it pass. The cycle is Red → Green → Refactor: 1) Write a test that fails (Red), 2) Write the minimum code to make it pass (Green), 3) Refactor the code while keeping tests passing.\n\nTDD benefits: Forces you to think about the interface before implementation, produces high test coverage naturally, catches regressions immediately, and creates living documentation (tests describe expected behavior). TDD works best for business logic and algorithms but may be overkill for UI code or rapid prototyping.\n\nTDD skepticism is common but misguided: the initial velocity decrease (writing tests first feels slower) is offset by dramatically reduced debugging time, fewer production bugs, and fearless refactoring ability.`, whyItMatters: `TDD produces code with naturally high test coverage, cleaner interfaces (you design the API from the consumer's perspective), and fewer production bugs. Teams practicing TDD consistently show lower defect rates.`, faqs: [{ question: 'What is TDD?', answer: 'Test-Driven Development: write a failing test first, then write code to pass it, then refactor. Red → Green → Refactor cycle. Produces clean code with high test coverage naturally.' }, { question: 'Is TDD slower?', answer: 'Initial velocity feels slower (writing tests first). But total velocity is faster: less debugging, fewer production bugs, faster refactoring. TDD is an investment that compounds over the project lifecycle.' }], relatedTerms: ['unit-testing', 'test-pyramid', 'shift-left-testing'] },
    { slug: 'contract-testing', title: 'Contract Testing', category: 'Testing & QA', definition: `Contract testing verifies that the interactions between service providers and consumers conform to a shared contract (API specification). Instead of testing the full integration, contract tests verify the interface agreement — what data shapes, status codes, and behaviors each side expects.\n\nTools: Pact (consumer-driven contract testing), Spring Cloud Contract (provider-side contracts), and OpenAPI-based contract testing. Pact workflow: 1) Consumer writes a test defining expected API behavior, 2) Pact generates a contract file, 3) Provider verifies it can satisfy the contract, 4) Both sides run independently.\n\nContract testing bridges the gap between unit tests (too isolated) and integration tests (too coupled). It enables independent service deployment — teams can deploy without coordinating with every consumer.`, whyItMatters: `In microservices, the most dangerous bugs are contract violations — a provider changes a response field and breaks 10 consumers. Contract testing catches these before deployment, enabling independent team velocity.`, faqs: [{ question: 'What is contract testing?', answer: 'Testing that verifies service interactions conform to a shared API contract. Catches interface changes (field renamed, type changed) before deployment. Bridges gap between unit and integration tests.' }, { question: 'Contract testing vs integration testing?', answer: 'Integration tests run services together (coupled, slow). Contract tests verify the interface agreement independently (decoupled, fast). Contract testing enables independent deployment; integration testing doesn\'t.' }], relatedTerms: ['integration-testing', 'api-versioning', 'microservices-communication'] },
    { slug: 'regression-testing', title: 'Regression Testing', category: 'Testing & QA', definition: `Regression testing verifies that previously working functionality still works after code changes. It catches "regressions" — bugs introduced by new code that break existing behavior. Regression testing is the safety net that enables continuous deployment.\n\nApproaches: Automated regression suite (run the full test suite on every deployment), Selective regression (run only tests affected by changed code — tools like Jest --changedSince), Visual regression (screenshot comparison to catch UI changes — Percy, Chromatic), and Smoke testing (quick subset of critical tests run immediately after deployment).\n\nRegression testing ROI: Without regression tests, every deployment requires manual verification of everything that could break. With regression tests, verification is automated, allowing daily (or hourly) deployments with confidence.`, whyItMatters: `Every line of code you change could break something else. Regression testing automates the verification that "everything still works." Without it, deployment speed is limited by manual testing capacity.`, faqs: [{ question: 'What is regression testing?', answer: 'Testing that verifies existing functionality still works after code changes. Catches "regressions" — new code breaking old behavior. The safety net that enables continuous deployment.' }, { question: 'How long should regression tests take?', answer: 'Target: < 15 minutes for the full suite. If longer, you need test optimization (parallelization, selective testing, faster infrastructure). Slow tests = slow deployments = slow feedback.' }], relatedTerms: ['test-pyramid', 'cicd', 'visual-regression-testing'] },
    { slug: 'visual-regression-testing', title: 'Visual Regression Testing', category: 'Testing & QA', definition: `Visual regression testing captures screenshots of UI components or pages and compares them pixel-by-pixel against baseline images to detect unintended visual changes. It catches CSS regressions, layout shifts, font changes, and responsive design issues that functional tests miss.\n\nTools: Percy (BrowserStack), Chromatic (Storybook), BackstopJS (open-source), and Playwright's built-in screenshot comparison. Workflow: 1) Capture baseline screenshots, 2) Run changed code, 3) Capture new screenshots, 4) Compare pixel-by-pixel, 5) Flag differences for human review.\n\nVisual regression testing is especially valuable for: design systems (ensuring consistency across components), responsive design (testing across breakpoints), and theme changes (verifying dark mode, accessibility modes don't break).`, whyItMatters: `CSS changes have unpredictable cascade effects. A single CSS change can break layouts across dozens of pages. Visual regression testing catches these graphical regressions that functional tests completely miss.`, faqs: [{ question: 'What is visual regression testing?', answer: 'Screenshot comparison testing that catches UI changes — CSS regressions, layout shifts, font changes. Compares current screenshots against baseline images pixel-by-pixel.' }, { question: 'Isn\'t this just screenshot testing?', answer: 'Visual regression is automated, diff-based screenshot testing integrated into CI/CD. It highlights only the pixels that changed, making review fast. Manual screenshot comparison doesn\'t scale.' }], relatedTerms: ['e2e-testing', 'regression-testing', 'design-system'] },
    { slug: 'load-testing', title: 'Load Testing & Performance Testing', category: 'Testing & QA', definition: `Load testing measures how a system performs under expected and peak traffic conditions. It identifies performance bottlenecks, memory leaks, and scalability limits before they affect real users.\n\nTypes: Load testing (expected traffic volume), Stress testing (beyond expected capacity — find the breaking point), Spike testing (sudden traffic surge), Soak testing (sustained load over hours — find memory leaks), and Chaos testing (failure injection under load).\n\nTools: k6 (Grafana, modern, JavaScript-based), Locust (Python-based, distributed), JMeter (Java-based, GUI-heavy), and Gatling (Scala-based, CI/CD friendly). Key metrics: response time (p50, p95, p99), throughput (requests/second), error rate, and resource utilization (CPU, memory, connections).`, whyItMatters: `Production performance issues are the most expensive bugs to fix (require immediate response, affect all users, damage reputation). Load testing finds them in staging — where they're cheap to fix — instead of production where they're a crisis.`, faqs: [{ question: 'What is load testing?', answer: 'Testing how a system performs under expected and peak traffic. Identifies bottlenecks, memory leaks, and scalability limits before they affect real users. Run in staging, not production.' }, { question: 'Which load testing tool should I use?', answer: 'k6 for modern teams (JavaScript, CI/CD friendly, open-source). Locust for Python teams. JMeter for complex scenarios (enterprise, legacy). Start with k6 if you don\'t have a preference.' }], relatedTerms: ['site-reliability-engineering', 'observability', 'chaos-engineering'] },
];
