import { GlossaryTerm } from '../types';
export const architecturePatternsTerms: GlossaryTerm[] = [
    { slug: 'domain-driven-design', title: 'Domain-Driven Design (DDD)', category: 'Architecture Patterns', definition: `Domain-Driven Design is a software design approach that centers the architecture around the business domain, using a shared language (ubiquitous language) between developers and domain experts. Created by Eric Evans, DDD structures software to mirror business processes.\n\nKey concepts: Bounded Context (clear boundary within which a domain model is consistent), Aggregate (cluster of entities treated as a single unit for data changes), Entity (object defined by identity, not attributes), Value Object (immutable object defined by attributes), Domain Event (something that happened in the domain that domain experts care about), and Repository (abstraction for data access).\n\nDDD is especially valuable for complex business domains where the cost of misunderstanding the domain outweighs the cost of additional architectural complexity.`, whyItMatters: `DDD prevents the most expensive software failures: building the wrong thing. By aligning code with business language and boundaries, DDD ensures that domain experts and developers share understanding.`, faqs: [{ question: 'What is DDD?', answer: 'Domain-Driven Design: a software design approach that aligns architecture with the business domain. Uses ubiquitous language, bounded contexts, and aggregates to mirror business processes in code.' }, { question: 'When should I use DDD?', answer: 'For complex business domains where misunderstanding costs more than additional architecture. Not for simple CRUD applications — the overhead isn\'t justified. Ideal for enterprise, fintech, and healthcare.' }], relatedTerms: ['event-driven-architecture', 'microservices-communication', 'cloud-architecture'] },
    { slug: 'event-sourcing', title: 'Event Sourcing', category: 'Architecture Patterns', definition: `Event sourcing stores every state change as an immutable event, building current state by replaying the event history. Instead of storing "the account balance is $500," you store every deposit and withdrawal. Current state is derived by replaying events in order.\n\nBenefits: Complete audit trail (every change is recorded), Time travel (reconstruct state at any point in time), Event replay (reprocess events with new business logic), and Natural for distributed systems (events are the communication mechanism).\n\nChallenges: Eventual consistency (reads may be stale), Event schema evolution (changing event formats over time), and Storage growth (events accumulate forever — use snapshots for performance).`, whyItMatters: `Event sourcing provides perfect auditability and the ability to reconstruct any historical state. Essential for financial systems, compliance-heavy domains, and systems where "why did this happen?" is a common question.`, faqs: [{ question: 'What is event sourcing?', answer: 'Storing every state change as an immutable event, then deriving current state by replaying events. Provides complete audit trail, time travel, and event replay capabilities.' }, { question: 'Event sourcing vs CRUD?', answer: 'CRUD stores current state (overwriting history). Event sourcing stores all state changes (preserving history). Use CRUD for simple domains. Use event sourcing when audit trails, temporal queries, or event replay are valuable.' }], relatedTerms: ['cqrs', 'event-driven-architecture', 'domain-driven-design'] },
    { slug: 'cqrs', title: 'CQRS (Command Query Responsibility Segregation)', category: 'Architecture Patterns', definition: `CQRS separates the read model (queries) from the write model (commands) into distinct data stores optimized for each purpose. Writes go to a normalized, consistent store. Reads come from denormalized, fast-query optimized stores. Events synchronize the two.\n\nBenefits: Read and write stores can be independently scaled, each store can be optimized for its use case (normalized for writes, denormalized for reads), enables different consistency models (strong consistency for writes, eventual for reads).\n\nCQRS is often paired with event sourcing: commands produce domain events (write side), events are projected into read-optimized views (read side). This combination enables auditability, scalability, and flexibility.`, whyItMatters: `CQRS solves the impedance mismatch between write requirements (data integrity, validation) and read requirements (speed, flexible queries). It enables independent scaling of reads and writes.`, faqs: [{ question: 'What is CQRS?', answer: 'Separating the read model from the write model into different stores. Writes use a normalized, consistent store. Reads use denormalized, query-optimized stores. Events synchronize both.' }, { question: 'When is CQRS appropriate?', answer: 'When read and write patterns differ significantly (many more reads than writes, or vice versa), when you need independent scaling, or when combined with event sourcing. Not for simple CRUD applications.' }], relatedTerms: ['event-sourcing', 'domain-driven-design', 'event-driven-architecture'] },
    { slug: 'saga-pattern', title: 'Saga Pattern', category: 'Architecture Patterns', definition: `The saga pattern manages distributed transactions across multiple microservices using a sequence of local transactions, each with a compensating action for rollback. Unlike traditional ACID transactions (which require a central coordinator), sagas use eventual consistency and compensation.\n\nSaga types: Choreography (each service emits events, other services react — no central coordinator, less coupling, harder to track), and Orchestration (a central saga orchestrator directs the flow — easier to understand, single point of coordination).\n\nExample: Order saga — 1) Create order (compensating: cancel order), 2) Reserve inventory (comp: release inventory), 3) Charge payment (comp: refund payment), 4) Ship order (comp: cancel shipment). If step 3 fails, compensating actions for steps 1-2 execute in reverse order.`, whyItMatters: `Distributed transactions (2PC) don't scale and tightly couple services. The saga pattern provides a scalable alternative for maintaining data consistency across microservices without distributed locks.`, faqs: [{ question: 'What is the saga pattern?', answer: 'Managing distributed transactions via a sequence of local transactions, each with a compensating (rollback) action. Provides consistency across microservices without distributed locks.' }, { question: 'Choreography vs orchestration sagas?', answer: 'Choreography: services react to events (no coordinator, more decoupled, harder to debug). Orchestration: central coordinator directs flow (easier to understand, single point of coordination). Most teams start with orchestration.' }], relatedTerms: ['microservices-communication', 'event-driven-architecture', 'domain-driven-design'] },
    { slug: 'strangler-fig-pattern', title: 'Strangler Fig Pattern', category: 'Architecture Patterns', definition: `The strangler fig pattern gradually replaces a legacy system by incrementally building new functionality around the old system, routing traffic from legacy to new components one piece at a time. Named after fig trees that grow around host trees, eventually replacing them entirely.\n\nProcess: 1) Identify a piece of legacy functionality to replace, 2) Build the replacement as a new service, 3) Route traffic for that functionality to the new service (using a proxy), 4) Verify the new service works correctly, 5) Retire the legacy code for that functionality, 6) Repeat until the legacy system is fully replaced.\n\nAdvantages over big-bang migration: lower risk (each step is small and reversible), continuous delivery (new features ship alongside migration), and incremental validation (prove the approach works before committing fully).`, whyItMatters: `Big-bang rewrites fail 80% of the time. The strangler fig pattern is the safest approach to legacy modernization — incremental, reversible, and value-delivering throughout the migration.`, faqs: [{ question: 'What is the strangler fig pattern?', answer: 'Incrementally replacing a legacy system by building new components around it and routing traffic piece by piece. Lower risk than big-bang rewrite because each step is small and reversible.' }, { question: 'How long does a strangler fig migration take?', answer: 'Depends on system size: 6-18 months for small systems, 2-5 years for large enterprise platforms. The key benefit: you deliver value continuously during migration, not just at the end.' }], relatedTerms: ['monolith-to-microservices', 'platform-consolidation', 'legacy-code'] },
    { slug: 'hexagonal-architecture', title: 'Hexagonal Architecture (Ports & Adapters)', category: 'Architecture Patterns', definition: `Hexagonal architecture (also called Ports and Adapters) structures applications so that the core business logic is isolated from external concerns (databases, APIs, UI). The core defines "ports" (interfaces) and external concerns implement "adapters" that plug into those ports.\n\nStructure: Core domain (business logic, no dependencies on frameworks or infrastructure), Ports (interfaces defined by the core — what it needs from the outside world), and Adapters (implementations of ports for specific technologies — PostgreSQL adapter, REST adapter, CLI adapter).\n\nBenefits: Testability (test core logic without databases or APIs — just mock the ports), Technology independence (swap databases, APIs, or UI frameworks without changing business logic), and Clear boundaries (forces separation between business rules and infrastructure).`, whyItMatters: `Hexagonal architecture prevents the most common cause of unmaintainable code: business logic entangled with database queries, API calls, and framework-specific code. Clean boundaries make testing, refactoring, and technology migration dramatically easier.`, faqs: [{ question: 'What is hexagonal architecture?', answer: 'An architecture where core business logic is isolated from external concerns using ports (interfaces) and adapters (implementations). Also called "Ports and Adapters." Enables technology independence.' }, { question: 'Hexagonal vs Clean Architecture vs Onion Architecture?', answer: 'All three are variations of the same core idea: isolate business logic from infrastructure. Hexagonal emphasizes ports/adapters. Clean Architecture adds use cases. Onion Architecture uses concentric layers. The principle is identical.' }], relatedTerms: ['domain-driven-design', 'cloud-architecture', 'test-pyramid'] },
    { slug: 'twelve-factor-app', title: 'Twelve-Factor App', category: 'Architecture Patterns', definition: `The Twelve-Factor App methodology (by Adam Wiggins/Heroku) defines 12 principles for building scalable, maintainable cloud-native applications:\n\n1. Codebase (one codebase tracked in VCS, many deploys)\n2. Dependencies (explicitly declare and isolate)\n3. Config (store in environment variables)\n4. Backing services (treat as attached resources)\n5. Build, release, run (strictly separate stages)\n6. Processes (stateless, share-nothing)\n7. Port binding (export services via port binding)\n8. Concurrency (scale out via process model)\n9. Disposability (fast startup, graceful shutdown)\n10. Dev/prod parity (keep environments similar)\n11. Logs (treat as event streams)\n12. Admin processes (run as one-off processes)\n\nThese principles are the foundation of modern cloud-native development. Applications that follow twelve-factor principles deploy easily to any cloud platform.`, whyItMatters: `Twelve-factor is the minimum bar for cloud-native applications. Violating these principles creates deployment friction, scaling limitations, and operational complexity.`, faqs: [{ question: 'What is a twelve-factor app?', answer: '12 principles for building scalable, portable cloud-native applications: one codebase, explicit dependencies, config in env vars, stateless processes, disposability, dev/prod parity, and more.' }, { question: 'Is twelve-factor still relevant?', answer: 'Absolutely. While the methodology is from 2012, the principles are foundational for Kubernetes, serverless, and container-based architectures. They remain the minimum bar for cloud-native development.' }], relatedTerms: ['cloud-architecture', 'kubernetes', 'serverless'] },
    { slug: 'modular-monolith', title: 'Modular Monolith', category: 'Architecture Patterns', definition: `A modular monolith is a single deployable application that is internally structured as well-defined, loosely coupled modules with clear boundaries. It combines the operational simplicity of a monolith with the organizational benefits of microservices.\n\nKey characteristics: Deployed as one unit (single process, single database), but internally organized as independent modules with defined APIs between them. Each module owns its data, has its own domain model, and communicates with other modules through explicit interfaces — not shared database tables.\n\nWhen to choose: Teams < 50 engineers, simpler operational requirements, when the overhead of microservices (service mesh, distributed tracing, per-service CI/CD) isn't justified. Many successful companies (Shopify, GitHub, Basecamp) run modular monoliths at massive scale.`, whyItMatters: `The modular monolith is often the right architecture when teams prematurely choose microservices. It provides module boundaries and team autonomy without the operational complexity of distributed systems.`, faqs: [{ question: 'What is a modular monolith?', answer: 'A single deployable application with well-defined internal modules. Combines monolith simplicity (one deployment, one database) with microservices organization (clear boundaries, owned data, defined APIs).' }, { question: 'Modular monolith vs microservices?', answer: 'Start with modular monolith. Extract microservices when you need independent scaling, different technology stacks, or team autonomy beyond what modules provide. Most companies extract too early.' }], relatedTerms: ['monolith-to-microservices', 'domain-driven-design', 'strangler-fig-pattern'] },
];
