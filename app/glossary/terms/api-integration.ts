import { GlossaryTerm } from '../types';
export const apiIntegrationTerms: GlossaryTerm[] = [
    { slug: 'rest-api', title: 'REST API', category: 'API & Integration', definition: `REST (Representational State Transfer) is an architectural style for designing networked applications. RESTful APIs use HTTP methods (GET, POST, PUT, DELETE) to perform CRUD operations on resources identified by URLs. REST has been the dominant web API paradigm since the mid-2000s.\n\nREST principles: Stateless (each request contains all information needed), Client-server separation, Uniform interface (resource-based URLs, standard HTTP methods), and Layered system (intermediaries like caches and load balancers). Richardson Maturity Model levels: Level 0 (single endpoint), Level 1 (resources), Level 2 (HTTP verbs), Level 3 (hypermedia/HATEOAS).`, whyItMatters: `REST APIs are the lingua franca of web services. Understanding REST design principles is essential for building maintainable, scalable, and developer-friendly APIs.`, faqs: [{ question: 'What is a REST API?', answer: 'An API that follows REST architectural principles: stateless, resource-based URLs, standard HTTP methods (GET/POST/PUT/DELETE), and JSON responses. The dominant web API paradigm.' }, { question: 'REST vs GraphQL?', answer: 'REST: simple, cacheable, well-understood, one resource per endpoint. GraphQL: flexible queries, single endpoint, client specifies data shape. Use REST for simple CRUD, GraphQL for complex data requirements.' }], relatedTerms: ['graphql', 'api-gateway', 'api-versioning'] },
    { slug: 'graphql', title: 'GraphQL', category: 'API & Integration', definition: `GraphQL is a query language and runtime for APIs, developed by Facebook (2012, open-sourced 2015). Unlike REST, where the server defines the response structure, GraphQL lets clients specify exactly which fields they need, reducing over-fetching and under-fetching.\n\nKey features: Single endpoint (all queries hit one URL), Client-specified queries (request exactly the data you need), Strong typing (schema defines all types and relationships), Real-time subscriptions (WebSocket-based live updates), and Introspection (API is self-documenting).\n\nTrade-offs: More complex server implementation, caching is harder (no URL-based caching), potential for expensive queries (N+1 problems, unbounded depth), and requires additional security measures (query depth/complexity limiting).`, whyItMatters: `GraphQL solves the mobile/frontend development problem of needing different data shapes for different views. One GraphQL query replaces multiple REST calls, reducing bandwidth and latency.`, faqs: [{ question: 'When should I use GraphQL?', answer: 'When clients need flexible data shapes (mobile apps, complex UIs), when you\'re aggregating multiple data sources, or when reducing network requests matters. Don\'t use for simple CRUD with one client.' }, { question: 'Is GraphQL replacing REST?', answer: 'No. Both coexist. REST is simpler for basic CRUD and has better caching. GraphQL shines for complex data requirements and multiple clients. Many organizations use both for different services.' }], relatedTerms: ['rest-api', 'api-gateway', 'api-versioning'] },
    { slug: 'webhooks', title: 'Webhooks', category: 'API & Integration', definition: `Webhooks are HTTP callbacks  -  automated messages sent from one application to another when a specific event occurs. Instead of polling (repeatedly asking "did anything change?"), webhooks push notifications in real-time.\n\nWebhook pattern: 1) Consumer registers a callback URL with the provider, 2) Event occurs in the provider system, 3) Provider sends an HTTP POST to the callback URL with event data, 4) Consumer processes the event and responds with 200 OK.\n\nBest practices: Verify webhook signatures (prevent spoofing), implement idempotency (handle duplicate deliveries), respond quickly (< 5 seconds) and process async, implement retry logic (exponential backoff), and log all webhook events for debugging.`, whyItMatters: `Webhooks enable real-time integration between systems without polling overhead. They're the backbone of modern SaaS integrations  -  Stripe payments, GitHub PR events, Slack notifications all use webhooks.`, faqs: [{ question: 'What is a webhook?', answer: 'An HTTP callback  -  an automated POST request sent from one system to another when an event occurs. Replaces polling ("did anything change?") with push notifications ("something changed!").' }, { question: 'How do you secure webhooks?', answer: 'Verify signatures (HMAC), validate the sender IP, use HTTPS only, implement idempotency keys (handle duplicates), and set up retry with exponential backoff for failed deliveries.' }], relatedTerms: ['rest-api', 'api-gateway', 'event-driven-architecture'] },
    { slug: 'api-versioning', title: 'API Versioning', category: 'API & Integration', definition: `API versioning is the practice of maintaining multiple versions of an API simultaneously to support existing clients while evolving the API for new capabilities. It's the contract management layer of API development.\n\nVersioning strategies: URL path versioning (/v1/users, /v2/users  -  most common, most explicit), Query parameter versioning (?version=2  -  flexible but less discoverable), Header versioning (Accept: application/vnd.api.v2+json  -  clean URLs, harder to test), and Content negotiation (different media types for different versions).\n\nVersioning policy: how long are old versions supported? Industry standard: 12-24 months of support after deprecation notice. Breaking changes (field removal, type changes, behavior changes) always require a new version.`, whyItMatters: `Breaking API changes without versioning destroys client trust and causes outages. A well-versioned API lets you evolve while maintaining backward compatibility  -  essential for platform businesses.`, faqs: [{ question: 'What is API versioning?', answer: 'Maintaining multiple API versions simultaneously to support existing clients while evolving for new ones. Prevents breaking changes from disrupting integrations.' }, { question: 'Which versioning strategy is best?', answer: 'URL path versioning (/v1/, /v2/) is most common and most explicit. It\'s easy to route, test, and document. Header-based versioning is cleaner but harder for developers to discover and test.' }], relatedTerms: ['rest-api', 'graphql', 'api-gateway'] },
    { slug: 'idempotency', title: 'Idempotency', category: 'API & Integration', definition: `An operation is idempotent if performing it multiple times produces the same result as performing it once. In distributed systems, idempotency is critical for handling retries safely  -  network failures and timeouts mean requests may be sent multiple times.\n\nHTTP method idempotency: GET (always idempotent), PUT (idempotent  -  setting a value to X twice = setting it once), DELETE (idempotent  -  deleting twice = deleting once), POST (NOT idempotent by default  -  creating a resource twice creates two resources).\n\nImplementing idempotency: Idempotency keys (client sends a unique ID with each request; server deduplicates by key), Last-write-wins (for update operations), and Database constraints (unique constraints prevent duplicate creation). Stripe uses idempotency keys for payment API  -  critical for preventing double charges.`, whyItMatters: `In distributed systems, exactly-once delivery is impossible. Operations will be retried. Without idempotency, retries cause duplicate records, double charges, and corrupted state. Idempotency turns "at-least-once" into "effectively-once."`, faqs: [{ question: 'What is idempotency?', answer: 'An operation is idempotent if doing it multiple times has the same effect as doing it once. Critical for safe retries in distributed systems. PUT and DELETE are naturally idempotent; POST is not.' }, { question: 'How do you make APIs idempotent?', answer: 'Idempotency keys (client provides a unique request ID, server deduplicates). For updates: use PUT with full resource representation. For creates: check for existing records before inserting.' }], relatedTerms: ['rest-api', 'webhooks', 'event-driven-architecture'] },
    { slug: 'event-driven-architecture', tier: 'pillar', title: 'Event-Driven Architecture', category: 'API & Integration', definition: `Event-driven architecture (EDA) is a design pattern where system components communicate by producing and consuming events  -  asynchronous notifications that something happened. Instead of direct request-response calls, services emit events that other services react to.\n\nPatterns: Event notification (simple notification that something happened), Event-carried state transfer (event contains the data, not just a reference), Event sourcing (store all events as the source of truth, derive current state from event history), and CQRS (separate read and write models, connected by events).\n\nTools: Apache Kafka (distributed event streaming), RabbitMQ (message broker), AWS SNS/SQS (managed messaging), and NATS (lightweight messaging). EDA enables loose coupling, scalability, and resilience  -  but adds complexity in debugging and maintaining event ordering.`, whyItMatters: `Event-driven architecture enables loose coupling between services, natural scalability (add consumers without changing producers), and temporal decoupling (producer and consumer don't need to be available at the same time).`, faqs: [{ question: 'What is event-driven architecture?', answer: 'A design pattern where components communicate via asynchronous events rather than direct calls. Services produce events (something happened) and other services consume and react to them.' }, { question: 'When should I use EDA?', answer: 'When services need loose coupling, when operations can be asynchronous, when you need audit trails, or when multiple services need to react to the same event. Don\'t use for simple synchronous request-response flows.' }], relatedTerms: ['webhooks', 'cloud-architecture', 'domain-driven-design'] },
    { slug: 'sdk', title: 'SDK (Software Development Kit)', category: 'API & Integration', definition: `An SDK (Software Development Kit) is a packaged set of tools, libraries, documentation, and code samples that enables developers to build applications for a specific platform, framework, or API. SDKs abstract away the complexity of raw API calls, providing language-native interfaces.\n\nSDK components: Client libraries (language-specific wrappers for API calls), Authentication helpers (handle OAuth, API keys, token refresh), Error handling (typed exceptions, retry logic), Documentation (getting-started guides, API reference), and Code samples (working examples for common use cases).\n\nSDK quality is a competitive differentiator for platform businesses. Stripe, Twilio, and AWS succeed partly because their SDKs are excellent  -  reducing time-to-first-API-call from hours to minutes.`, whyItMatters: `SDKs are the developer's first experience with your platform. A great SDK reduces time-to-integration from days to hours. A poor SDK drives developers to competitors. For platform businesses, SDK quality directly impacts adoption.`, faqs: [{ question: 'What is an SDK?', answer: 'A Software Development Kit  -  packaged tools, libraries, and documentation for building on a specific platform. SDKs abstract raw API complexity into language-native interfaces.' }, { question: 'How many language SDKs should we support?', answer: 'Minimum viable: JavaScript/TypeScript and Python (covers 70%+ of developers). Add Go, Java, and Ruby based on your audience. Each SDK requires maintenance  -  don\'t support more than you can keep updated.' }], relatedTerms: ['rest-api', 'api-gateway', 'developer-experience'] },
    { slug: 'oauth', title: 'OAuth 2.0', category: 'API & Integration', definition: `OAuth 2.0 is an authorization framework that enables third-party applications to access user resources without exposing credentials. It's the industry standard for API authorization  -  powering "Sign in with Google," "Connect to GitHub," and virtually all third-party API integrations.\n\nOAuth 2.0 flows: Authorization Code (most secure, for server-side apps), PKCE (secure extension for mobile/SPA apps), Client Credentials (machine-to-machine, no user context), and Device Code (for devices without browsers, like CLI tools and TVs).\n\nKey concepts: Access tokens (short-lived, authorize API access), Refresh tokens (long-lived, obtain new access tokens), Scopes (permissions requested), and OIDC (OpenID Connect, identity layer on top of OAuth for authentication).`, whyItMatters: `OAuth 2.0 is the foundation of API security. Any application that integrates with third-party services or provides API access to third parties needs OAuth. Implementing it incorrectly creates severe security vulnerabilities.`, faqs: [{ question: 'What is OAuth 2.0?', answer: 'An authorization framework that lets third-party apps access user resources without credentials. Powers "Sign in with Google" and virtually all API integrations. Not authentication  -  that\'s OIDC on top of OAuth.' }, { question: 'OAuth vs API keys?', answer: 'API keys identify the application. OAuth authorizes the application to act on behalf of a user. Use API keys for server-to-server calls without user context. Use OAuth when users need to grant access to their data.' }], relatedTerms: ['zero-trust', 'api-gateway', 'rest-api'] },
    { slug: 'api-rate-design', title: 'API Design Principles', category: 'API & Integration', definition: `API design principles are guidelines for creating APIs that are intuitive, consistent, and developer-friendly. Good API design reduces integration time, lowers support burden, and increases platform adoption.\n\nCore principles: Resource-oriented design (nouns in URLs: /users, /orders  -  not verbs: /getUsers), Consistent naming conventions (camelCase or snake_case, pick one), Meaningful HTTP status codes (200 success, 201 created, 400 bad request, 404 not found, 429 rate limited), Pagination for collections, Filtering and sorting via query parameters, and Comprehensive error responses (error code, message, documentation link).\n\nAPI design review checklist: Is it intuitive (can a developer guess the endpoint without docs)? Is it consistent (same patterns everywhere)? Is it secure (authentication, authorization, input validation)? Is it evolvable (versioning strategy, backward compatibility)?`, whyItMatters: `APIs are the product for platform businesses. A well-designed API reduces time-to-integration by 5-10x. Poor API design creates permanent support burden because breaking changes require versioning.`, faqs: [{ question: 'What makes a good API?', answer: 'Intuitive resource naming, consistent patterns, meaningful status codes, pagination, filtering, comprehensive errors, and clear documentation. If a developer can guess the endpoint, you\'ve designed well.' }, { question: 'REST API naming: nouns or verbs?', answer: 'Nouns. Use /users (not /getUsers), /orders (not /createOrder). HTTP methods provide the verbs: GET /users (list), POST /users (create), GET /users/123 (read), PUT /users/123 (update).' }], relatedTerms: ['rest-api', 'graphql', 'developer-experience', 'sdk'] },
    { slug: 'microservices-communication', title: 'Microservices Communication Patterns', category: 'API & Integration', definition: `Microservices communication patterns define how distributed services exchange data and coordinate work. Choosing the right pattern for each interaction is critical for system reliability, performance, and maintainability.\n\nSynchronous patterns: REST/gRPC (request-response, simple but couples services temporally), Service mesh (manages inter-service communication with retries, circuit breaking, and observability), and API gateway (aggregates multiple service calls into single client response).\n\nAsynchronous patterns: Message queues (point-to-point: RabbitMQ, SQS), Event streams (pub-sub: Kafka, EventBridge), and Saga pattern (distributed transactions across services using compensating actions).\n\nPattern selection: Use sync for queries needing immediate response. Use async for commands that can be eventually consistent. Use sagas for distributed transactions. Use event sourcing for audit-critical operations.`, whyItMatters: `Wrong communication patterns cause cascade failures (sync calls to a slow service block the caller), data inconsistency (distributed transactions without sagas), and debugging nightmares (async events without tracing).`, faqs: [{ question: 'Sync or async for microservices?', answer: 'Use sync (REST/gRPC) when the caller needs an immediate response. Use async (Kafka/SQS) when the operation can be eventually consistent. Most systems use both  -  sync for reads, async for writes.' }, { question: 'How do you handle transactions across microservices?', answer: 'Saga pattern: a sequence of local transactions where each step has a compensating action for rollback. Avoid distributed transactions (2PC)  -  they don\'t scale and couple services tightly.' }], relatedTerms: ['service-mesh', 'event-driven-architecture', 'cloud-architecture', 'domain-driven-design'] },
];
